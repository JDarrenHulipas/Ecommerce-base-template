{
  "name": "bakerycloud-api",
  "version": "0.1.0",
  "description": "REST API multi-tenant para BakeryCloud (Node.js + Express + PostgreSQL)",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:api": "node --test tests/api.test.js tests/admin.test.js",
    "test:admin": "node --test tests/admin.test.js",
    "test:e2e": "node --test tests/e2e.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
